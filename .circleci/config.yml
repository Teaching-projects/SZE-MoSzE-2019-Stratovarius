version: 2
jobs:
  build:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - checkout
      - run: 'g++ -std=c++11 main.cpp cmd.cpp'
      - run: 'make'
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
